services:
  app:
    image: ${APP_IMAGE:?APP_IMAGE is required}
    container_name: ${APP_NAME:?APP_NAME is required}
    restart: unless-stopped
    networks:
      - default

networks:
  default:
    driver: bridge
